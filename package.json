{
  "name": "bookmi-ui-testing",
  "private": true,
  "version": "0.1.0",
  "description": "Codelab: from brittle selectors to intent-driven UI testing, on the Bookmi (Qorelly) demo app",
  "scripts": {
    "dev": "npm run dev --prefix demo-app",
    "build": "npm run build --prefix demo-app",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
